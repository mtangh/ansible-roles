---
# defaults file for role4
# vim: set ff=unix ts=2 sw=2 sts=2 et : This line is VIM modeline

vars_for_platforms:
  default: yes

combine_vars_for_platforms:
  from_platforms_default: yes

force_overwrite_vars_for_platforms: from_platform_default

