---
# role* tasks file
# vim: set ff=unix ts=2 sw=2 sts=2 et : This line is VIM modeline

- name: "{{ tests_casename }} | {{ tests_rolename }} | redhat_fedora"
  fail:
    mag: "Undefined variable."
  when: vars_for_platforms|d(none) is not none

