---
# role* tasks file
# vim: set ff=unix ts=2 sw=2 sts=2 et : This line is VIM modeline

- name: "{{ tests_casename }} | {{ tests_rolename }} | redhat_centos_6"
  fail:
    mag: "Undefined variable."
  when: vars_for_platforms.RedHat_CentOS_6|d(false)|bool == false

