---
# defaults file for role4
# vim: set ff=unix ts=2 sw=2 sts=2 et : This line is VIM modeline

vars_for_platforms:

vars_for_platforms_is_not_empty_list:
- from
- role_defaults

combine_vars_for_platforms:
  from_role_defaults: yes

force_overwrite_vars_for_platforms: from_role_defaults

